# Template — NÃO commitar valores reais.
# Crie o Secret real via kubectl (ver README, Passo 4) em vez de aplicar este arquivo
# com a chave preenchida. Mantido aqui só como referência de shape.
apiVersion: v1
kind: Secret
metadata:
  name: docs-mcp-secrets
  namespace: default
type: Opaque
stringData:
  # Chave do Google Generative AI (Gemini) — provider dos embeddings.
  GOOGLE_API_KEY: "REPLACE_ME"
